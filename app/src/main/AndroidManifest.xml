<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tdp_basico_posta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.InstructionsActivity"></activity>
        <activity android:name=".ui.RoundsActivity" />
        <activity android:name=".ui.WinnerActivity" />
        <activity android:name=".InitialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Playing" />
        <activity android:name=".ui.AddChallengeActivity" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>